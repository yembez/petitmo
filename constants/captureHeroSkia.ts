/** Flou sur les bords de la photo hero Capturer (Skia). */
export const CAPTURE_HERO_EDGE_BLUR = 16;
/** Début du fondu masque (0 = centre, 1 = bord) — plus bas = zone nette plus large. */
export const CAPTURE_HERO_EDGE_MASK_INNER = 0.5;
/** Rayon du masque radial (× max(largeur, hauteur)). */
export const CAPTURE_HERO_EDGE_MASK_RADIUS_RATIO = 0.82;
