/**
 * Interrupteurs produit — features prêtes mais désactivées pour une release.
 * Remettre à `true` pour réactiver sans retoucher le code métier.
 */

/**
 * V1 off : multi-sélection photos → toujours **un post par photo**.
 * Conservé : modale album vs séparés + `uploadPhotoAlbum` (plusieurs photos / même post).
 */
export const IMPORT_MULTI_PHOTO_ALBUM_ENABLED = false;
